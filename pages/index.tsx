import Card from "@components/common/Card";

import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <h1>Welcome to the boilerplate</h1>
      <Card>This component is just made as a default setup</Card>
    </div>
  );
};

export default Home;
